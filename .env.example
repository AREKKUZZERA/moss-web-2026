# Optional overrides. Leave empty on Vercel to use vercel.json rewrites.
# VITE_MOSS_API_BASE=/api/moss
# VITE_TABLE_API_BASE=/api/table

# Vercel server-side upstreams. Set these in the Vercel project environment
# when the default backend address changes; do not expose them as VITE_* vars.
# MOSS_API_UPSTREAM=http://213.21.57.115:8080/moss
# TABLE_API_UPSTREAM=http://213.21.57.115:8787
